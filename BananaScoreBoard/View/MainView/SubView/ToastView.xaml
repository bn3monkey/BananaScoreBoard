<Page x:Class="BananaScoreBoard.View.MainView.SubView.ToastView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:BananaScoreBoard.View.MainView.SubView"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ToastView">

    <Grid>
        <Border Background="#dddddd" CornerRadius="10">
            <Border.Child>
                <TextBlock Text="{Binding Toast, UpdateSourceTrigger=PropertyChanged}"
                    HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                    Margin="10,10,10,10" FontSize="10"/>
            </Border.Child>
        </Border>
    </Grid>
</Page>
